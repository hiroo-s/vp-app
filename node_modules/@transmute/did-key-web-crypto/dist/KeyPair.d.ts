import { KeyPair as WebKeyPair } from '@transmute/web-crypto-key-pair';
import { GenerateKeyOpts, JsonWebKey2020 } from '@transmute/web-crypto-key-pair/dist/types';
export declare class KeyPair extends WebKeyPair {
    static fingerprintFromPublicKey(publicKey: JsonWebKey2020): Promise<string>;
    static from: (keypair: JsonWebKey2020) => Promise<KeyPair>;
    static fromFingerprint: (opts: {
        fingerprint: string;
    }) => Promise<KeyPair>;
    static generate: (opts?: GenerateKeyOpts | undefined) => Promise<KeyPair>;
    fingerprint(): Promise<string>;
    toKeyPair(exportPrivateKey?: boolean): Promise<any>;
    deriveSecret(remote: JsonWebKey2020): Promise<Uint8Array>;
}
